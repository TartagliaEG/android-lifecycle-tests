<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.lifecycletests">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >

        <activity
            android:name=".simple.SimpleActivity">
        </activity>
        <activity
            android:name=".simple.SSimpleActivity"
            android:theme="@style/Theme.AppCompat.DayNight">
        </activity>

        <activity
            android:name=".retained.RetainedFlowActivity">
        </activity>
        <activity
            android:name=".retained.SRetainedFlowActivity"
            android:theme="@style/Theme.AppCompat.DayNight">
        </activity>

        <activity
            android:name=".backstack.BackStackFlowActivity">
        </activity>
        <activity
            android:name=".backstack.SBackStackFlowActivity"
            android:theme="@style/Theme.AppCompat.DayNight">
        </activity>

        <activity
            android:name=".utils.ResultActivity">
        </activity>
        <activity
            android:name=".utils.DialogActivity"
            android:theme="@style/Theme.AppCompat.Dialog">
        </activity>

        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.DayNight">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>